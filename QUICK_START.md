#  Быстрый старт - CUDA Image Processing

##  Что уже сделано

1.  **CUDA библиотека** - 3 фильтра (Grayscale, Rotation, Blur)
2.  **Пакетная обработка** - обычная и конвейерная (pipeline)
3.  **Веб-интерфейс** - красивый UI с Drag & Drop
4.  **Документация** - подробное техническое описание алгоритмов

---

##  Запуск веб-интерфейса (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Убедитесь что проект скомпилирован

```bash
cd /home/anton/PProgramming_MultithreadedImageProcessing/build
cmake --build .
```

### Шаг 2: Запустите веб-сервер

```bash
cd ../web
./start_server.sh
```

### Шаг 3: Откройте браузер

```
http://localhost:5000
```

###  Возможности веб-интерфейса:

- **📁 Drag & Drop** - перетаскивайте изображения
- ** Оттенки серого** - преобразование в ч/б
- **🔄 Поворот** - ползунок от 0° до 360° (шаг 90°)
- **🌫 Размытие** - ползунок радиуса от 1 до 20
- ** Статистика** - время выполнения на GPU
- **👀 Сравнение** - До/После
- ** Скачивание** - результата

---

##  Командная строка (CLI)

### Обработка одного изображения:

```bash
cd /home/anton/PProgramming_MultithreadedImageProcessing

# Оттенки серого
./build/image_processor grayscale foto/image.png output/gray.png

# Поворот
./build/image_processor rotate90 foto/image.png output/rotated.png

# Размытие
./build/image_processor blur foto/image.png output/blurred.png 10
```

### Пакетная обработка:

```bash
# Обычная (многопоточная)
./build/image_processor batch grayscale ./foto ./output

# Конвейерная (БЫСТРЕЕ на 20-30%)
./build/image_processor batch-pipeline grayscale ./foto ./output
```

---

##  Производительность

**Тест:** 20 изображений ~1280×720 пикселей

| Метод | Время | Ускорение |
|-------|-------|-----------|
| CPU (1 поток) | ~15 сек | 1× |
| GPU + CPU (batch) | ~2.5 сек | 6× |
| GPU + CPU (pipeline) | ~2.1 сек | **7.1×** |

---

## 📁 Структура проекта

```
PProgramming_MultithreadedImageProcessing/
├── src/                    # Исходный код CUDA
│   ├── core/              # Ядро (GPU память, kernels)
│   ├── filters/           # Фильтры (grayscale, rotation, blur)
│   ├── utils/             # Утилиты (загрузка, batch)
│   └── main.cu            # CLI программа
├── web/                   # Веб-интерфейс
│   ├── app.py            # Flask сервер
│   ├── templates/        # HTML
│   ├── static/           # CSS + JS
│   └── start_server.sh   # Скрипт запуска
├── tests/                 # Юнит-тесты
├── build/                 # Скомпилированные файлы
├── foto/                  # Тестовые изображения
└── README.md              # Полная документация (552 строки!)
```

---

##  Технологии

**Backend (CUDA):**
- CUDA 12.3
- C++17
- CMake
- GCC 12

**Frontend (Web):**
- Python 3 + Flask
- HTML5 + CSS3
- Vanilla JavaScript
- Drag & Drop API

**GPU:**
- NVIDIA RTX 4060 (3072 CUDA cores)
- Compute Capability 8.6

---

## 📖 Документация

- **README.md** - Полная техническая документация с алгоритмами (552 строки)
- **web/README_WEB.md** - Документация веб-интерфейса
- **QUICK_START.md** - Этот файл

---

## 🎓 Для защиты курсовой

### Что показать:

1. **Веб-интерфейс** - загрузить изображение, применить фильтр
2. **Статистику** - время выполнения на GPU (~0.1-0.5 сек)
3. **Сравнение До/После** - визуальный результат
4. **Код** - показать CUDA kernel (например, grayscale)
5. **Архитектуру** - объяснить конвейерную обработку

### Ключевые моменты:

-  **Параллелизм**: каждый пиксель = 1 поток GPU
-  **Ускорение**: в 7 раз быстрее CPU
-  **Конвейер**: 3 этапа работают одновременно
-  **CUDA Streams**: 4 изображения обрабатываются параллельно
-  **Оптимизации**: асинхронные операции, переиспользование памяти

---

## 🐛 Решение проблем

### Веб-сервер не запускается:

```bash
cd web
source venv/bin/activate
pip install Flask Werkzeug
python3 app.py
```

### CUDA программа не найдена:

```bash
cd build
cmake .. -DCMAKE_CUDA_HOST_COMPILER=/usr/bin/g++-12 \
         -DCMAKE_C_COMPILER=/usr/bin/gcc-12 \
         -DCMAKE_CXX_COMPILER=/usr/bin/g++-12
cmake --build .
```

### Проверка GPU:

```bash
nvidia-smi
nvcc --version
```

---

##  Готово!

Проект **полностью готов** к защите! 

-  Работающий код
-  Веб-интерфейс
-  Документация
-  Тесты
-  Примеры

**Удачи на защите! **

