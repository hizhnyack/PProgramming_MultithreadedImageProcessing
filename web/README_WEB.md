# 🌐 CUDA Image Processing - Веб-интерфейс

Красивый веб-интерфейс для обработки изображений на GPU через браузер!

## 🎨 Возможности

- **Drag & Drop** - перетаскивайте изображения прямо в браузер
- **Предпросмотр** - просмотр исходного изображения перед обработкой
- **5 фильтров** - Grayscale, Rotate 90°/180°/270°, Blur
- **Настройка параметров** - регулируйте радиус размытия
- **Сравнение До/После** - наглядное сравнение результатов
- **Статистика** - время выполнения и размер файла
- **Скачивание** - сохранение обработанных изображений

## 🚀 Запуск

### 1. Убедитесь что проект скомпилирован:

```bash
cd ../build
cmake --build .
cd ../web
```

### 2. Запустите веб-сервер:

```bash
./start_server.sh
```

### 3. Откройте браузер:

```
http://localhost:5000
```

## 📁 Структура

```
web/
├── app.py                 # Flask сервер (backend)
├── templates/
│   └── index.html         # HTML интерфейс
├── static/
│   ├── style.css          # Стили
│   └── script.js          # JavaScript логика
├── uploads/               # Временные загруженные файлы
├── output/                # Обработанные изображения
├── requirements.txt       # Python зависимости
├── start_server.sh        # Скрипт запуска
└── README_WEB.md          # Эта документация
```

## 🔧 Технологии

**Backend:**
- Python 3
- Flask (веб-фреймворк)
- subprocess (вызов CUDA программы)

**Frontend:**
- HTML5
- CSS3 (градиенты, анимации, grid)
- Vanilla JavaScript (без фреймворков)
- Drag & Drop API
- Fetch API

## 📊 Как это работает

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Браузер   │─────▶│    Flask    │─────▶│    CUDA     │
│  (Frontend) │      │  (Backend)  │      │  Program    │
└─────────────┘      └─────────────┘      └─────────────┘
      ▲                     │                     │
      │                     │                     │
      │                     ▼                     ▼
      │              [uploads/]            [GPU Processing]
      │                     │                     │
      │                     ▼                     │
      │              [output/] ◀──────────────────┘
      │                     │
      └─────────────────────┘
         (результат)
```

### Процесс обработки:

1. **Пользователь** загружает изображение через браузер
2. **JavaScript** отправляет файл на Flask сервер (AJAX)
3. **Flask** сохраняет файл во временную папку `uploads/`
4. **Flask** вызывает CUDA программу: `./build/image_processor filter input output`
5. **CUDA** обрабатывает изображение на GPU
6. **Flask** возвращает результат в браузер
7. **JavaScript** отображает результат пользователю

## 🎯 API Endpoints

### `GET /`
Главная страница

### `POST /upload`
Загрузка и обработка изображения

**Параметры:**
- `file` - файл изображения (multipart/form-data)
- `filter` - название фильтра (grayscale, rotate90, etc.)
- `blur_radius` - радиус размытия (только для blur)

**Ответ:**
```json
{
    "success": true,
    "message": "Успешно обработано",
    "output_file": "uuid_output.png",
    "execution_time": 0.123,
    "file_size": 456.78
}
```

### `GET /download/<filename>`
Скачивание обработанного файла

### `GET /view/<filename>`
Просмотр обработанного файла

### `GET /filters`
Получить список доступных фильтров

## 🎨 Дизайн

- **Градиентный фон** - фиолетовый градиент
- **Карточки фильтров** - интерактивные с hover эффектами
- **Анимации** - плавные переходы и загрузка
- **Адаптивный** - работает на мобильных устройствах
- **Современный** - Material Design inspired

## 🔒 Безопасность

- Проверка типов файлов (только PNG, JPG, BMP)
- Ограничение размера (макс. 16 МБ)
- Уникальные имена файлов (UUID)
- Автоматическое удаление временных файлов
- Timeout для обработки (30 сек)

## 💡 Советы

1. **Для лучшей производительности** используйте изображения до 2000×2000 пикселей
2. **Для размытия** рекомендуемый радиус: 5-10
3. **Для batch обработки** используйте CLI версию (`batch-pipeline`)

## 🐛 Отладка

Если сервер не запускается:

```bash
# Проверьте что CUDA программа скомпилирована
ls -la ../build/image_processor

# Проверьте Python зависимости
source venv/bin/activate
pip list | grep Flask

# Запустите вручную
python3 app.py
```

## 📝 Лицензия

Курсовой проект по параллельному программированию  
Команда из 4 студентов

